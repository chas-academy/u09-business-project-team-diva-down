import card_container from "../card_container";

const Leaderboard = () => {
    return (
        <>
            <h2>Leaderboard</h2>
            <p>The current leaderboards of the players, regarding all time and of the week</p>
        </>
    );
};

export default card_container(Leaderboard);