import card_container from "../card_container";

const PlayerStats = () => {
    return (
        <>
            <h2>Player Stats</h2>
            <p>Check in on your own player stats, and see how you rank compared to other players</p>
        </>
    );
};

export default card_container(PlayerStats);