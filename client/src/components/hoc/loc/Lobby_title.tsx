import under_title_comp from "../under_title_comp";

const Lobby_title = () => {
    return (
        <>
            <h2 id="singleplayer_main_title">Lobby</h2>
        </>
    );
};

export default under_title_comp(Lobby_title);