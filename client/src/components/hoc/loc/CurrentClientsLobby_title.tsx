import title_comp from "../title_comp";

const lobby_title = () => {
    return (
        <>
            <h2 id="singleplayer_main_title">Lobby</h2>
        </>
    );
};

export default title_comp(lobby_title);